{
  "name": "kit-search",
  "version": "1.0.0",
  "description": "Get tracking info for wastewater sample kits",
  "main": "server/server.js",
  "scripts": {
    "install-all": "npm install && cd client && npm install",
    "seed": "node server/seed.js",
    "server": "node server/server.js",
    "client": "cd client && npm start",
    "server-client": "npm run server & npm run client",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Timothy Barmann",
  "license": "ISC",
  "dependencies": {
    "cors": "2.8.5",
    "express": "4.18.2",
    "prop-types": "15.8.1",
    "sqlite3": "5.1.4"
  },
  "engines": {
    "npm": "^8.0.0",
    "node": "^16.0.0"
  }
}
